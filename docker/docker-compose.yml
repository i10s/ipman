# docker-compose file for running the service
# File: /docker/docker-compose.yml

version: '3'
services:
  python-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - CONSUL_HOST=10.121.109.180
    networks:
      - app-network

networks:
  app-network:
